# Rendered output

The app writes generated transparent sticker PNGs to this directory.

Most rendered images are ignored by Git because they are local build artifacts.

The repository keeps one published sample output image, [`hug.png`](hug.png), as a reference result for the README.

